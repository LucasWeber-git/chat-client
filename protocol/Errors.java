package protocol;

import static protocol.Protocol.SEPARATOR;
import static protocol.ProtocolProperties.ERROR;

public class Errors {

    public static final String DUPLICATED_USER = ERROR + SEPARATOR + "DUPLICATED_USER";

}
